进入内核源码目录（kernel）。
打开配置菜单：
make menuconfig


找到以下路径（不同内核版本路径可能略有不同，但大同小异）：
Kernel hacking
-> printk and dmesg options
-> Default console loglevel (默认控制台日志级别)

修改数值：
默认通常是 7 (debug) 或者 4 (warning)。
把它改成 8。

#重新编译kernel并打包
cd ..
./build.sh kernel
./build.sh firmware
./build.sh updateimg


#不行的话在终端用


# 修改 printk 打印级别，让所有消息都直接显示
echo 8 > /proc/sys/kernel/printk

#或者单独看每个指令：
dmesg | tail



export ARCH=arm64
make menuconfig

adb push '/home/wzp/BSP/01_helloworld/helloworld.ko'  /home/topeet/BSP
